---
import Bomb from '../assets/playa.png'
import Colina from '../assets/colina.png' 
import Poste from '../assets/poste.png'
import Folder from '../assets/folder.svg'
import Savings from '../assets/savings.svg'
import Gift from '../assets/gift.svg'
import Mapa from '../assets/mapa.png'
import Mockup from '../assets/mockupcel.png'
import Ubicacion from '../assets/ubicacion.png'

---


<section class="product-hero">
    <div class="container">
        <div class="d-table">
            <div class="d-table-cell center">
                <div class="hero-copy">
                    <h1 class="h1-title">Conecta, Explora y Crece con XicoNemi</h1>
                    <p> Descubre una nueva forma de explorar tu ciudad, conectar con negocios locales y aprovechar eventos exclusivos. Con XicoNemi, siempre estarás al tanto de lo mejor que tu comunidad tiene para ofrecer.</p>
                    <div class="product-cta">
                      <a href="#" class="button button-primary u-full-width">Descarga la App</a>
                    </div>
                </div>
            </div>
        </div>
        <div class="hero-media">
            <img src={Poste} alt="Poste">
            <img src={Colina} alt="Colina">
            <img src={Mapa} alt="Mapa">
            <img src={Ubicacion} alt="Ubicacion">
            <img src={Mockup} alt="Mockup">
        </div>
    </div>
 </section>

 <style lang="scss">


.product-hero{
  padding: 50px 0;
  .container{
    position: relative;
    .d-table{
      height: 500px;
     }
  }
  .hero-media{
    padding: 5px;
    border-radius: 48px;
  }
  .hero-copy{
    h1, p{
      max-width: 600px;
      margin: 20px auto;
    }
  }
  .hero-media{
    img{
      position: absolute;
      width: 160px;
      &:nth-child(1){
        top: 0;
        left: -100px;
      }
      &:nth-child(2){
        top: 0;
        right: -100px;
      }
      &:nth-child(3){
        top: 250px;
        left: 0;
      }
      &:nth-child(4){
        top: 250px;
        right: 0;
      }
      &:nth-child(5){
        bottom: -20px;
        left: 50%;
        transform: translateX(-50%);
      }
    }
    @media (max-width:700px) {
      img{
        &:nth-child(2), &:nth-child(3),
        &:nth-child(4){
          display: none;
        }
      }
    }
  }
}

 </style>